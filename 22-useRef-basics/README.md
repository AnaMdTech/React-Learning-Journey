# 22 - useRef Basics

This mini-project demonstrates how to use `useRef` in React to:

- Focus input elements
- Directly manipulate DOM elements
- Persist values without re-rendering

## 🔍 Concepts Covered

- Using `useRef()` to access DOM elements
- Avoiding re-renders with ref
- Managing focus and style via DOM